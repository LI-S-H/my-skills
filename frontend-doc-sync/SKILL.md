---
name: frontend-doc-sync
description: 前端契约同步。用于 UI 改动影响 API/schema、DTO、Mock、测试数据、校验规则、状态模型、页面展示字段或 PRD 字段说明时保持一致。
---

# 前端契约同步

1. 阅读 `references/doc-sync-checklist.md`。
2. 找出变化的字段、状态、动作、校验规则、响应结构和派生展示值。
3. 同步最权威契约、API 适配层、Mock/测试数据和必要文档，并运行可用契约检查。